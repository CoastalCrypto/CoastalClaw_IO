{
  "name": "@coastal-ai/shell",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "package": "tsc && electron-builder build --config build/electron-builder.yml",
    "dev": "tsc && CC_DEV=1 electron dist/main.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^30.0.0",
    "electron-builder": "^24.0.0",
    "typescript": "^5.4.0"
  }
}
